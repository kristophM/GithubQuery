# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GithubQuery' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GithubQuery
  pod 'Alamofire', '~> 4.0'
  pod 'Toucan', '~> 0.6'
  pod 'SDWebImage', '~>3.8'
  target 'GithubQueryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GithubQueryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
